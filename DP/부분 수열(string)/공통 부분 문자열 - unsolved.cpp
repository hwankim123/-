#include <iostream>
#include <vector>
using namespace std;

#define MAXS 4001

char s1[MAXS], s2[MAXS];
int cache[MAXS][MAXS];

int main()
{
    cin >> s1 >> s2;
    int i = 0, j = 0;
    while (s1[i] != '\0')
    {
        while (s2[j] != '\0')
        {
        }
    }
}