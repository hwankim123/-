#include <iostream>
#include <queue>
#include <vector>
using namespace std;

#define MAXN 101
#define INF 987654321

int T;
int N, M, K;
vector<pair<int, int>> adj[MAXN];

int main()
{
    cin >> T;
    for (int tc = 0; tc < T; tc++)
    {
        cin >> N >> M >> K;
        for (int i = 0; i < K; i++)
        {
            int u, v, c, d;
            cin >> u >> v >> c >> d;
        }
    }
}